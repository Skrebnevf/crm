= styles_for :signed_job

= render 'show_title_bar'

.remote#options{ hidden }

= render 'signed_job_details'

= render 'request_for_quatation_details'

= render 'signed_job_totals'

%br

= link_to(t(:signed_jobs), signed_jobs_path, remote: true)
